/*
#include<iostream>
using namespace std;

int main()
{
	//三目运算符
	//函数： 表达式1 ？ 表达式2 : 表达式3
	//如果表达式1为真，则执行表达式2，并返回表达式2的结果
	//如果表达式1为假，则执行表达式3，并返回表达式3的结果
	
	// 创建三个变量 a b c， 将a、b作比较，将大的赋值给c
	int a = 10 , b = 20 , c = 0;
	c = (a >= b ? a : b);
	cout << c << endl;

	// C++中三目运算符返回的是变量，可以继续赋值
	(a < b ? a : b) = 100;
	cout << "a = " << a << endl;
	cout << "b = " << b << endl;

	system("pause");

	return 0;
}
*/
